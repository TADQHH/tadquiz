---
// Placeholder shell — UI replaced by design implementer.
import '../../../styles/global.css';
import { getFormBySlug } from '../../../db/forms.mjs';

const form = getFormBySlug(Astro.params.slug ?? '');
if (!form) return Astro.redirect('/404');
---

<!doctype html>
<html lang="vi" data-theme="tad">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Đã gửi — {form.title}</title>
  </head>
  <body>
    <main id="main" class="page-shell py-12">
      <h1 class="section-title">Đã gửi</h1>
      <p class="lede">Cảm ơn bạn đã hoàn thành "{form.title}".</p>
    </main>
  </body>
</html>
